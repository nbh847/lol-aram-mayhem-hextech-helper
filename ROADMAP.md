# 项目路线图

更新时间：2026-08-13

## 当前阶段

运行环境已准备完成，进入真实功能与可交付验证。

核心桌面应用、英雄识别、OCR 分析、数据更新和 PyInstaller 打包代码已经存在；当前主要缺口是本地运行环境、真实功能验证和自动化测试。

## 已完成

- 已补齐项目规则入口：`AGENTS.md`、`CLAUDE.md`。
- 已建立本路线图，作为项目进度记录源。
- 已完成源码结构检查：10 个 Python 文件通过 AST 语法解析。
- 已确认数据快照包含 30,631 条海克斯记录，覆盖 173 个英雄。
- 已确认推荐打包入口为 `build.py`，运行入口为 `gui_launcher.py`。
- 已通过 `uv` 安装用户级 Python 3.12.13，并在项目内创建 `.venv`。
- 已为 `.venv` 补齐 `pip`、`setuptools` 和 `wheel`；项目依赖尚未安装。
- 已将 `requirements.txt` 安装到 `.venv`，共安装 48 个包。
- 已通过 `pip check`，15 个关键依赖导入成功，项目核心模块导入成功。
- 已修复 `one_click_update()` 同步缺陷：GitHub 下载成功后不再跳过 `sync_official_data()`，确保本地 `champions.json` 始终同步 Riot 官方 233 个英雄列表。
- 已修复旧包中 `蒸汽机器人` 被错误映射为 `Jade_Blitzcrank` 导致 OP.GG 404 与 ChromeDriver 崩溃的问题，验证源数据为 `Blitzcrank` 并重新打包。
- 已修复 `sync_official_data()` 接收 Riot 皮肤变体污染的根因：Data Dragon 16.15.1 返回 233 条含 60 条 `Jade_` 前缀皮肤变体（如 `Jade_Nunu` 对应旧名「雪人骑士」），会覆盖标准英雄映射并污染 URL。改为过滤所有含下划线 key，保留 173 个真英雄。
- 已新增「全量冷却」保护：抽样校验检测到差异触发全量前，检查距上次全量时间，不足 24 小时则跳过（日志提示用「全量更新」按钮强制更新），避免网络抖动误判导致的短时重复全量爬取。
- 已新增「全量更新」按钮二次确认：点「全量更新」时若距上次全量不足 24 小时，弹窗提示「数据可能已最新，是否仍要强制全量？」，用户取消则不执行，确认则强制全量。保留强制出口的同时避免误触浪费时间。
- 已改为「全量更新」永远弹窗确认：不再依赖 config.json 是否存在，三种场景区分文案（24h 内全量过/24h 前全量过/从未全量过）。
- 已新增爬虫类更新模式前置 Chrome 检测：`full`/`smart`/`spot_check`/`patch`/`precise` 触发前调 `is_chrome_installed()`（注册表 + 常见路径 + PATH 三级检测），未装 Chrome 弹窗提示安装或改用一键更新。`one_click` 不检测（优先 GitHub 在线下载）。
- 已新增海克斯推荐图片 UI 渲染框架（`main.py`）：新增 `OVERLAY_REGIONS` 常量定义下方图片位置；`OverlayApp` 新增 `_load_templates()`（从 `assets/templates/` 加载 PNG，按 `tier_棱彩.png` 命名）、`_select_template()`（按等级选模板）、`_render_image_card()`（PIL 贴文字到模板并显示）；模板缺失时静默降级为纯文字。现有排名文字位置上移到「图标和名字中间」（`base_y - 60`）。
- 已根据 `data/lol-hex-view.png`（2560×1440 实机截图）校准覆盖层位置：文字改为卡片水平居中并放在图标与名称之间；图片区域下移至描述文字下方空白区，使用 `H * 0.14` 偏移和 `H * 0.115` 高度。
- 已将 `C:\Users\ronnie\Pictures\ai\lol-3hex.png` 的三列模板拆分为三张紧凑推荐状态图片，保存至 `assets/templates/`；覆盖层改为按普通推荐/最优推荐/异常错误选择绿色、金色、红色模板。
- 已新增 `scripts/preview_overlay.py` 静态截图预览工具：跳过游戏进程、LCU 和管理员检查，直接对 `data/lol-hex-view.png` 执行 OCR 并全屏显示截图与覆盖层；支持 `--hero` 进行本地英雄数据匹配，按 Esc 退出。
- 已将静态预览改为 Win32 分层透明覆盖窗口，绕过当前环境不可用的 Tcl/Tk 初始化；覆盖层置顶、鼠标穿透，不替换当前桌面，并通过 ORB + RANSAC 将参考截图坐标映射到图片查看器中的实际显示位置，允许右侧窗口部分遮挡。
- 已将正式覆盖层布局统一为 `2560×1440` 参考画布 + 等比缩放 + 居中偏移；OCR 区、推荐文字、图片区域和字体共用同一缩放基准，避免不同宽高比分别按屏幕宽高缩放造成横向或纵向漂移。

## 进行中

- 暂无代码实现中的进行项。

## 待办

1. 在真实 Windows 环境完成 GUI、F6/F7/F8、OCR、LCU 英雄识别和悬浮遮罩冒烟验证。
2. 执行 `python build.py`，检查生成目录中的 EXE、`data/` 和 `assets/` 是否完整，并在干净目录启动一次。
3. 为数据加载、英雄搜索、排序推荐和更新合并逻辑补充最小自动化测试。

## 阻塞

- 系统默认 `python` 仍指向 Python 3.8；本项目必须使用 `.venv` 中的 Python 3.12.13。
- 暂无已证实的依赖安装阻塞；真实 GUI/OCR/LCU 验证仍需要 Windows 图形环境和英雄联盟客户端。

## 最近验证

- 2026-08-04：实现爬虫类更新模式前置 Chrome 检测：`hero_scraper.is_chrome_installed()` 三级检测（注册表/路径/PATH），实测当前机器返回 True；GUI `_select()` 对爬虫模式拦截；重新打包 `dist\build_1785932841\`，字节码确认函数和路径字符串编入。
- 2026-08-13：读取 `data/lol-hex-view.png` 实机截图（2560×1440），完成文字和图片覆盖层的相对位置校准；项目源码 10 个 Python 文件通过 AST 语法解析。
- 2026-08-13：基于 `lol-3hex.png` 生成并接入三种紧凑推荐状态模板；3 张图片尺寸约为 1740×904，10 个 Python 文件 AST 解析通过。
- 2026-08-13：新增静态截图 OCR/覆盖层预览脚本；截图 OCR 识别三个海克斯标题成功，`--hero txj` 成功匹配「探险家」并将第二张标为最佳推荐；11 个 Python 文件 AST 解析通过。
- 2026-08-13：Tk/Tcl 初始化失败后改用 Win32 透明分层窗口预览；实际桌面截图确认图片查看器、右侧 Codex 窗口及任务栏均保留原样，仅三组文字和图片叠加在对应卡片上；预览进程已退出。
- 2026-08-13：统一多分辨率布局变换；1280×720、1920×1080、2560×1440、3840×2160、1920×1200、2560×1600、3440×1440 七种分辨率的三列间距、文字到图片区间距及图片区尺寸比例断言通过。
- 2026-08-04：修复 `one_click_update()` 中错误的 `from scripts.config import update_pinyin_file`（该函数实际定义在 updater.py 自身），删除多余 import 改为直接调用。该 bug 导致 GitHub 在线下载成功后必然抛 ImportError 降级到本地爬取。冒烟验证 import 链打通，重新打包 `dist\build_1785931741\`，字节码确认无错误 import。
- 2026-08-04：实现「全量更新」按钮二次确认（方案C）：`UpdateDialog._select()` 在 full 模式触发前检查距上次全量时间，不足 24 小时弹窗让用户确认；从 EXE 内嵌 CArchive 提取 `gui_launcher` 字节码，确认含二次确认文案；重新打包 `dist\build_1785853068\`，并复制最新全量数据 (29984 行)。
- 2026-08-04：实现「全量冷却」保护（方案A）：`run_update()` 的 `spot_check` 分支在触发全量前检查距上次全量时间，不足 24 小时则跳过；四种场景单元验证通过（从未全量/1h前/25h前/24h边界）；重新打包 `dist\build_1785851777\`，字节码已含冷却逻辑文案。
- 2026-08-04：定位并修复 `sync_official_data()` 接收 Riot 皮肤变体污染根因（Data Dragon 16.15.1 返回 60 条 `Jade_` 前缀条目）；改为过滤含下划线 key，保留 173 个真英雄；重新打包 `dist\build_1785849994\`，全面验证包内 `champions.json` 与源码一致、无 `Jade_` 污染，修复文案已编入 PYZ 字节码。
- 2026-08-04：修复 `one_click_update()` 同步缺陷并通过 AST 语法解析；重新打包生成 `dist\build_1785846668\`，验证包内 `蒸汽机器人` 映射为 `Blitzcrank`。
- 2026-08-03：修复 Selenium 版本兼容性问题（降级到 4.15.0），数据爬虫功能正常。
- 2026-08-03：修改 GitHub 下载地址指向 nbh847 仓库，形成数据分发闭环。
- 2026-08-03：完成 PyInstaller 打包，生成 246.5 MB 可执行文件（已清理不必要文件）。
- 2026-08-02：Python 源码 AST 解析通过；未执行 GUI、OCR、LCU 或 PyInstaller 运行验证。
- 2026-08-02：确认项目根目录当前没有可交付的 `dist/` 构建产物。
- 2026-08-02：`.venv` 已验证使用 Python 3.12.13，且项目目录已忽略该虚拟环境。
- 2026-08-02：依赖安装完成；`pip check`、关键依赖导入、项目模块导入和 AST 解析均通过。
